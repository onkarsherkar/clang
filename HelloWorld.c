#include <stdio.h>
#include<math.h>

int main(){
  
  /// Math Function

  double A = sqrt(9);
  double B = pow(2, 4);
  int C = round(3.25);
  int D = ceil(4.33);
  int E = floor(5.55);
  double F = fabs(-25);
  double G = log(3);
  double H = sin(45);
  double I = cos(45);
  double J = tan(45);

  printf("%0.2lf\n", J);

  return 0;
}